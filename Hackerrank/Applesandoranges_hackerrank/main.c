#include <stdio.h>
#include <stdlib.h>

int main()
{
    int start, end, apple, orange, apple_count, orange_count, apple_fall, orange_fall;
    scanf("%d %d %d %d %d %d", &start, &end, &apple, &orange, &apple_fall, &orange_fall);
    int *apple_pos, *orange_pos;
    apple_pos = (int*)malloc(sizeof(int)*apple_fall);
    orange_pos = (int*)malloc(sizeof(int)*orange_fall);
}
